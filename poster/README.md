# poster

Poster materials and LaTeX theme assets used for project presentations.
This directory is intentionally self-contained and does not affect the
Lean libraries or proofs.

To rebuild a poster, use the local `Makefile` (if present) or compile
`poster.tex` with your LaTeX toolchain.
